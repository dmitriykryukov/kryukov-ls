.avatar {
  display: block;
  overflow: hidden;
  border-radius: 50%;
  box-shadow: 0px 10px 24px 0 rgba(#000, .3);
  flex-shrink: 0;
}

.avatar__pic {
  display: block;
  object-fit: cover;
  width: 100%;
  height: 100%;
}